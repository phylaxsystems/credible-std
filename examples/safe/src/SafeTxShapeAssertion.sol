// SPDX-License-Identifier: MIT
pragma solidity ^0.8.13;

// The maintained implementation lives in src/. This source unit deliberately reuses that symbol
// so existing example imports resolve to the exact same contract and creation bytecode.
import {SafeTxShapeAssertion} from "credible-std/protection/safe/SafeTxShapeAssertion.sol";
